version: '1'
services:
  api:
    image: best-movies-api
    build: .
    networks:
      - backend
    ports:
      - "3000:3000"
    volumes:
      - ./:/opt/api

  web-cli:
    image: best-movies-api
    networks:
      - backend
    command: sh

networks:
  backend:
    driver: bridge